apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-blue
  namespace: project4
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user
      version: blue
  template:
    metadata:
      labels:
        app: user
        version: blue
    spec:
      containers:
        - name: user
          image: yourdockerhub/user-service:v1
          ports:
            - containerPort: 3001