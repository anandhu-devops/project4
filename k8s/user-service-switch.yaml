apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: project4
spec:
  selector:
    app: user
    version: blue   # switch to green later
  ports:
    - port: 3001
      targetPort: 3001